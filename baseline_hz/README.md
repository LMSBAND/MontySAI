# Frozen baseline: the Hz-tolerance era

Every model and verdict produced under pitch_hz tolerance 15.0 Hz
(octave-VARIANT: 24 cents at C5, 190 at C3). Code state: commit
2c8a4c0 and earlier -- the cents change is the next commit after
this freeze. Takes: ~/Desktop/chords/django/ (unchanged on disk).

This is the reference build for the unit surgery. Every cents-era
number gets compared against THESE files, not against memory.
